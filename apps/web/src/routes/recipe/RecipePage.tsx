import PlaceholderPage from '../../components/PlaceholderPage';

export default function RecipePage() {
  return (
    <PlaceholderPage
      emoji="🍳"
      title="Recipes"
      blurb="Import a recipe from a URL or add your own. Coming in Phase 1 — see .claude/recipe.md."
    />
  );
}
