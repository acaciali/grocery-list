import PlaceholderPage from '../../components/PlaceholderPage';

export default function InventoryPage() {
  return (
    <PlaceholderPage
      emoji="🥫"
      title="Pantry"
      blurb="Log what you have so recipes and the grocery list can get smart about it. Coming in Phase 1 — see .claude/inventory.md."
    />
  );
}
