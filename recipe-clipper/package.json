{
  "name": "recipe-clipper",
  "private": true,
  "version": "0.1.0",
  "description": "Chrome extension that clips a recipe from the current page.",
  "//": "type=commonjs opts out of the root package.json's type=module -- the extension scripts are plain classic scripts loaded by <script> tags, and the tests use require().",
  "type": "commonjs",
  "scripts": {
    "test": "node test/recipe-parser.test.js && node test/form-utils.test.js"
  }
}
