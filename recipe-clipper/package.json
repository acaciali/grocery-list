{
  "name": "@grocery/recipe-clipper",
  "private": true,
  "version": "0.1.0",
  "description": "Chrome extension that clips a recipe from the current page into the recipe box.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@grocery/shared": "*",
    "firebase": "^10.13.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^2.1.5"
  }
}
