{
  "name": "grocery-list",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*",
    "functions"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "emulators": "npm run build -w functions && firebase emulators:start --only firestore,auth,functions"
  },
  "devDependencies": {
    "firebase-tools": "^13.29.1",
    "typescript": "^5.6.3"
  }
}
